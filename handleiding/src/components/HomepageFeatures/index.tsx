import type { ReactNode } from "react";

// This component is kept for potential future use
// Currently not displayed on the homepage

export default function HomepageFeatures(): ReactNode {
  return null;
}
